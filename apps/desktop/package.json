{
  "name": "@adea-ai/desktop",
  "version": "0.22.0",
  "private": true,
  "description": "Adea desktop lane: the Electrobun shell plus the single-UI client build it serves",
  "type": "module",
  "scripts": {
    "shell:client:build": "bun scripts/client.mjs",
    "shell:build": "bun scripts/shell.mjs build",
    "shell:dev": "bun scripts/shell.mjs dev",
    "lint": "oxlint",
    "test": "bun test tests",
    "typecheck": "tsc -p shell/tsconfig.json"
  }
}
