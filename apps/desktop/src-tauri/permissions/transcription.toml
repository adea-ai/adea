"$schema" = "../gen/schemas/schema.json"

[[permission]]
identifier = "allow-desktop-transcription"
description = "Allows only the bundled main window to request and control an ephemeral macOS system dictation session."
commands.allow = [
  "desktop_transcription_permission",
  "desktop_transcription_start",
  "desktop_transcription_cancel",
]
