{
  "name": "@agent-hq/mobile",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "shell:dev": "cap open",
    "shell:build": "cap sync",
    "sync": "cap sync",
    "add:android": "cap add android",
    "add:ios": "cap add ios",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@agent-hq/config": "workspace:*",
    "@capacitor/core": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0",
    "@types/node": "^26.2.0"
  }
}
