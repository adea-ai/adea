[package]
name = "shell-bench-tauri"
version = "0.1.0"
edition = "2021"
description = "M5.2 throwaway parity shell — deleted by #371"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[profile.release]
strip = true
