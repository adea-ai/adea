# M12 #396/#397 packaged acceptance evidence — 2026-09-22

This record captures one fresh packaged run from the isolated checkout
`feat/m12-396-397-packaged-evidence`. It is evidence for the open re-closure
gates on #396 (terminal sidecar/replay) and #397 (worktree containment), not a
milestone-closure claim.

## Command and build

```text
bun install --frozen-lockfile
bun run test:packaged
```

The command built the DEV macOS app and ran the real bundled layout at:

```text
apps/desktop/shell/build/dev-macos-arm64/Adea-dev.app
```

The packaged lane summary reports `status: passed`, duration `46431ms`, and
the following proof results:

| Proof                                | Result                         | Artifact                                            |
| ------------------------------------ | ------------------------------ | --------------------------------------------------- |
| supervision smoke                    | pass                           | `artifacts/packaged/supervision-smoke.json`         |
| terminal replay/restart/resync       | pass                           | `artifacts/packaged/terminal-replay.json`           |
| transport defect probe               | warning: defect not reproduced | `artifacts/packaged/terminal-transport-defect.json` |
| worktree containment/template tamper | pass                           | `artifacts/packaged/worktree-containment.json`      |
| browser/device matrix                | pass                           | `artifacts/packaged/browser-matrix.json`            |

The JSON artifacts are generated under the git-ignored `artifacts/` directory
by design. The durable lane summary is
`artifacts/dev-runtime/packaged-summary.json`.

## Acceptance facts recorded

- The sidecar was staged inside the app bundle and launched on the bundled Bun
  runtime. Supervision adoption, identity re-proof, crash-loop bounds, and
  managed-Pi packaged absence/install truth all passed.
- A fresh host adopted the same packaged sidecar after the first host exited.
  The PTY session survived, below-ring durable replay returned every chunk once
  and in order, and seeded retention GC produced the deterministic resync
  anchor with zero partial replay frames.
- The production worktree registrar created real worktrees below the
  authorized root, materialized a promoted template, refused a same-size and
  restored-mtime template tamper with `identity_mismatch`, cloned nothing from
  the tampered template, and rejected a stale lease generation.
- The transport defect probe's pure-socket loss reproduction no longer matched
  the sidecar wire path. The lane intentionally records this as a warning; the
  required terminal replay proof passed independently.

## Remaining gates

This run does **not** prove the full re-closure checklist:

- #396 still needs packaged owner-journey evidence that the terminal pane
  writes through the authority gate, plus visual baseline review and the
  24-hour/input-to-paint endurance records.
- #397 still needs packaged create → approved bootstrap → merge-back →
  complete-and-clean through the production registrar, plus the packaged
  include-copy escape proofs and worktree-pane visual baseline review.
- The two packaged Bun tests run before the app build are expected to skip when
  no bundle exists; after this run, the standalone packaged scripts are the
  authoritative executed evidence. Manual native accessibility/visual review
  was not performed by this headless lane.
