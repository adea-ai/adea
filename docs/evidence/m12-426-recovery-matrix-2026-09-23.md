# M12 #426 recovery matrix — 2026-09-23

Evidence for #426's completion box: _"Offline/reconnect/restart/update/rollback/disk-full/permission-loss and partial-cleanup recovery are proven."_ Each row names the scenario, the pin that proves it, and what the proof actually exercises — a scenario without a named pin would be a gap, not a row.

| Scenario            | Pin                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | What it exercises                                                                                                                                                                                                                                                                                        |
| ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Offline**         | `packages/types/tests/execution-location.test.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Offline, revoked, unknown, and stale locations resolve to explicit `queued` / `blocked` states with remediation data, and "does not silently relocate an unavailable local default to self-hosted" — an unavailable location can never appear executable, and nothing fails over to the cloud implicitly |
| **Reconnect**       | `workspace-events.test.ts` ("replays strictly after a cursor in sequence order and stops at the head"; "a lost wake-up never loses the durable event"); `apps/web/e2e/dev-view-terminal.spec.ts` ("terminal reconnects after a bounded transport flap and survives a split"); the pane lane's scripted sidecar restart                                                                                                                                                                                                                                                                             | Durable cursor replay with exactly-once catch-up, and a transport that reattaches rather than fabricating continuity                                                                                                                                                                                     |
| **Restart**         | `supervision-boot-reconcile.test.ts` ("a sidecar launch persisted by a previous app run is adopted"; "a launch this supervisor already owns is skipped, never clobbered"; the same suite's state-independence pin); `scripts/dev-runtime-terminal-soak.mjs` crash/restart replay                                                                                                                                                                                                                                                                                                                   | A boot adopts what the previous run left, refuses what it cannot re-prove, and leaves Agent HQ's own encrypted store and vault byte-identical                                                                                                                                                            |
| **Update**          | `release-assets.yml` staging/verify gates; `packages/dev-view` release lockstep; the packaged lane's manifest (`artifacts/packaged/supervision-smoke.json` resolves every component with its digest)                                                                                                                                                                                                                                                                                                                                                                                               | The shipped artifact carries the components it claims, with digests recorded for diagnostics                                                                                                                                                                                                             |
| **Rollback**        | `updater-rollback.test.ts` ("restores the previous version and quarantines the failed artifact"; "refuses an implicit rollback when no previous install is staged"; "refuses to restore an incomplete previous install"; "the planner supplies the explicit rollback target for a supervised component")                                                                                                                                                                                                                                                                                           | An executed rollback, not a planner-only claim: the failed artifact is quarantined with its bytes retained, the previous install is restored exactly, and a torn or absent previous install is refused                                                                                                   |
| **Disk-full**       | `dev-runtime-managed-pi.test.ts` ("a failing cache write under disk pressure never fails the install"; "a failing install write is typed retryable, leaves no partial install, and a retry succeeds"; "a failed reinstall keeps the previous managed version serving")                                                                                                                                                                                                                                                                                                                             | ENOTDIR stands in for ENOSPC on the same code path (documented in the test): the cache degrades, the install fails typed and retryable with nothing partial left behind, and the previous version keeps serving                                                                                          |
| **Permission-loss** | `dev-runtime-computeruse.test.ts` ("a TCC grant that moves to denied stops input at the first frame after the freshness window"); `packaged-computeruse-tcc.ts` (denied accessibility routes to the exact Settings pane with `open_settings` remediation); `dev-runtime-vault-key-roles.test.ts` ("the production keychain adapter reads exactly the vault slot and refuses foreign-sized keys")                                                                                                                                                                                                   | A revoked OS permission stops action at the next admitted frame, surfaces guidance instead of failing silently, and a credential slot that cannot be read is refused rather than approximated                                                                                                            |
| **Partial cleanup** | `worktree-service.test.ts` ("preflight blocks destructive plans on unpushed and leased state"; "ambiguous interrupted quarantine stays recovery_required after restart"; "the trash sweep drains quarantined entries left by cleanup"); `worktree-bootstrap-leases-templates.test.ts` ("heartbeat, suspect, expiry, release, and the destructive gate"); `dev-runtime-resource-adapters.test.ts` ("maps clean, pushed, lease, and archive facts onto the policy contract"); the terminal soak's attach/detach churn (`scripts/dev-runtime-terminal-soak.mjs`, 411 churn cycles in the current run) | Cleanup is two-phase and journal-backed: preflight refuses to destroy leased or unpushed state, an interrupted pass is `recovery_required` rather than silently abandoned, the sweep drains what a partial pass quarantined, and churn leaves no accumulated descriptors or listeners                    |

## Bounds and honesty

- The **24-hour soak** that this matrix shares a box-context with is a separate evidence item: the corrected run is in flight (`bun run test:soak:dev-runtime` with `ADEA_DEV_RUNTIME_SOAK_DURATION_MS=86400000`); its summary carries `budgetHonored: true` when the wall-clock budget actually bound — the defect that made a 24-hour claim possible from 47 seconds was fixed in #625.
- **Disk-full** is simulated as a failing write on the production path, not by filling a volume. The property under test is the recovery contract, which is volume-state independent; a real ENOSPC run on a filled volume is not part of this evidence and is named here rather than implied.
- **Permission-loss** covers the OS permission surfaces this tile touches (screen capture/accessibility consent, the keychain slot). A **manual** screen-reader/keyboard pass remains a recorded gap on #541 and is out of scope for this matrix.
