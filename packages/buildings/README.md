# Buildings package

The package exposes the optional building-field runtime used by the shared
scene host. Agent HQ's Home and Work scenes use their authored foundations and
do not ship a separate building catalog.
