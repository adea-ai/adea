# Characters package

This package provides the generic character-provider and animation-controller
interfaces. The active HQ character models and embedded animation clips are
owned by `@agent-hq/ithappy`.
