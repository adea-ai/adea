# Characters package

This package owns the Agent HQ character runtime and assets: built-in character
models with embedded animation clips, customizable character parts and
wearables, shared animation loading, and the provider/controller interfaces
used by the scene runtime.
