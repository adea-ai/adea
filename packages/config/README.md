# Adea Config

Shared workspace configuration for Adea. The package owns the canonical
TypeScript base configuration; application and package code stays
in its own workspace. Linting and formatting are owned by the root
.oxlintrc.json / .oxfmtrc.json baseline (oxlint/oxfmt).
