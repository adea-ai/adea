# Interior package

This package combines Agent HQ's interior room layout, room-designer prop
types/runtime, interior model catalog, and validation schemas. It owns the
interior furniture, food, casino, and plant assets used inside Home and Work.

Exterior foliage, trees, and fences belong to `@agent-hq/landscape` instead.
