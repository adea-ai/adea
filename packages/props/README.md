# Props package

This package owns the generic prop manifest types and field-loader boundary.
The HQ room designer catalog is supplied by `@agent-hq/models`, so no separate
prop-model catalog is shipped here.
