# Vehicles package

This package keeps the optional vehicle-controller interfaces used by the
shared scene runtime. The current HQ asset boundary does not ship vehicle
models; Home and Work use ithappy characters and furniture instead.
